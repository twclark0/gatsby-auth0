import React from "react";

import { rhythm } from "../utils/typography";

const Bio = () => {
  return (
    <div
      style={{
        display: `flex`,
        marginBottom: rhythm(2.5),
      }}
    ></div>
  );
};

export default Bio;
